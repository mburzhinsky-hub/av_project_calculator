# TEST REPORT — AV Project Calculator v0.3.1

Дата проверки: 2026-05-29

## Проверено
- Распаковка ZIP.
- Наличие `package.json`, `src`, `index.html`, `README.md`.
- Чистая установка зависимостей через `npm install`.
- Production-сборка через `npm run build`.
- Локальный запуск Vite dev server.
- Проверка отдачи приложения по `http://127.0.0.1:5173/`.
- Проверка HTML-мокапа: синтаксис JavaScript и базовый рендер таблиц/расчётов через DOM-модель.

## Исправлено
- Улучшено чтение булевых полей при импорте спецификации: `да/нет`, `true/false`, `1/0`, `yes/no`.
- Поля `Требуется монтаж`, `Требуется ПНР`, `Требуется проектирование`, `Признак критической позиции` теперь корректнее распознаются при импорте Excel.

## Результат
- `npm install` — успешно.
- `npm run build` — успешно.
- `npm run dev` — успешно.
- Критических ошибок сборки не обнаружено.

## Замечание
Vite предупреждает о крупном JS-бандле из-за `xlsx`, `recharts` и общей сборки в один файл. Для MVP это некритично. Для версии 1.0 желательно разделить код на lazy-loaded модули.
